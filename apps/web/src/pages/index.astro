---
import '../styles/global.css';

const games = [
  {
    name: 'Coral Wars',
    slug: 'coral-wars',
    description: 'Guerre tactique sous les récifs',
    players: '2',
    duration: '20min',
    emoji: '🪸'
  }
];
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Onirigate</title>
  </head>
  <body class="min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-blue-950">
    <main class="container mx-auto px-4 py-16">
      <header class="text-center mb-20">
        <h1 class="text-7xl font-bold mb-6 bg-gradient-to-r from-cyan-300 via-blue-400 to-purple-400 bg-clip-text text-transparent">
          Onirigate
        </h1>
        <p class="text-xl text-slate-300">
          Jouez en ligne, sans inscription
        </p>
      </header>

      <section class="grid gap-6 max-w-4xl mx-auto">
        {games.map(game => (
          <a 
            href={`/${game.slug}`}
            class="group relative bg-slate-800/40 backdrop-blur-sm rounded-2xl p-8 border border-slate-700/50 hover:border-cyan-500/50 transition-all duration-300 hover:scale-[1.02] hover:shadow-2xl hover:shadow-cyan-500/10"
          >
            <div class="flex items-center gap-6">
              <div class="text-6xl flex-shrink-0 group-hover:scale-110 transition-transform">
                {game.emoji}
              </div>
              
              <div class="flex-1">
                <h2 class="text-3xl font-bold mb-2 text-white group-hover:text-cyan-400 transition">
                  {game.name}
                </h2>
                <p class="text-slate-400 mb-4">{game.description}</p>
                <div class="flex gap-4 text-sm text-slate-500">
                  <span class="bg-slate-700/50 px-3 py-1 rounded-full">{game.players} joueurs</span>
                  <span class="bg-slate-700/50 px-3 py-1 rounded-full">{game.duration}</span>
                </div>
              </div>

              <div class="text-cyan-400 text-xl group-hover:translate-x-2 transition-transform">
                →
              </div>
            </div>
          </a>
        ))}
      </section>

      <footer class="text-center mt-20 text-slate-500 text-sm">
        <p>Gratuit • Sans compte • Open source</p>
      </footer>
    </main>
  </body>
</html>